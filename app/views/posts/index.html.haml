%div.container.border.mt-5.pt-3.col-8
  %h1
    タイムライン
  - @posts.each do |post|
    %div.card.mb-3
      %div.card-body
        %p.card-text
          = post.content
        %p.card-text.text-end
          = post.created_at.strftime('%Y/%m/%d %H:%M')

%div.container.border.mt-5.pt-3.col-8
  %h1
    投稿フォーム
  = form_with(model: @post, local: true, html: { class: 'mb-3' }) do |form|
    .form-group
      = form.text_area :content, class: 'form-control', rows: 3
    = form.submit '投稿する', class: 'btn btn-primary'
