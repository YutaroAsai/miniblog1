%div.container.border.mt-3.pt-3.col-8
  %h4
    タイムライン
  - @posts.each do |post|
    %div.card.mb-3
      %div.card-body
        %p.card-text
          = post.content
        %p.card-text.text-end
          = post.created_at.strftime('%Y/%m/%d %H:%M')

%div.container.border.my-3.py-3.col-8
  %h4
    投稿フォーム
  = form_with model: @post, local: true, data: { turbo: false } do |form|
    .form-group
      = form.text_area :content, class: 'form-control my-3', rows: 3
    = form.submit '投稿する', class: 'btn btn-primary'
